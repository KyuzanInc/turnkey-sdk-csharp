{
  "name": "turnkey-sdk-csharp-fixture-generators",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Reproducible upstream compatibility fixtures for the C# SDK.",
  "engines": {
    "node": ">=20.10.0 <25"
  },
  "scripts": {
    "preflight": "node generate-stamper-vectors.mjs --preflight-only",
    "gen:stamper": "node generate-stamper-vectors.mjs",
    "gen:all": "npm run gen:stamper"
  },
  "dependencies": {
    "@noble/curves": "1.3.0",
    "@turnkey/api-key-stamper": "0.5.0"
  },
  "overrides": {
    "@noble/curves": "1.3.0",
    "@noble/hashes": "1.3.3"
  }
}
